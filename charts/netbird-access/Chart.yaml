apiVersion: v2
name: netbird-access
description: NetBird CRs exposing in-cluster services over WireGuard (router + resources + wrappers), plus optional consumer-side sidecar injection, configurable per cluster
type: application
version: 0.5.0
appVersion: 0.6.0
