apiVersion: v2
name: netbird-access
description: NetBird CRs (router + resources + wrappers) exposing in-cluster services over WireGuard, configurable per cluster (DEV-123)
type: application
version: 0.3.0
appVersion: 0.6.0
